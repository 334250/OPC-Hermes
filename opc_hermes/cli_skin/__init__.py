"""OPC-Hermes CLI theming and subcommands.

Registers `opc` subcommand group via Hermes plugin CLI hooks:
  - opc list     — browse registered agents and skills
  - opc status   — show active workflow status
  - opc config   — manage OPC configuration
"""
