"""OPC-Hermes independent WebUI (React + FastAPI).

Provides:
  - Workflow visualization (DAG graph, agent status)
  - Agent List browser (search, capability view, score history)
  - Optimization proposal review + approval
  - Artifact browser (output files, version history)
  - Memory Inspector (per-agent context viewer)
"""
